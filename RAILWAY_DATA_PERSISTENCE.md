# Railway 데이터 유지 가이드

## ⚠️ 현재 상황

**현재 저장 방식:** JSON 파일 기반 (`data/` 폴더)
- `data/sessions.json` - 음성 채널 세션 기록
- `data/timeSlots.json` - 시간 슬롯별 참여 기록

## 🚨 문제점: 재배포 시 데이터 손실 위험

Railway에서 재배포 시 데이터가 **날아갈 수 있습니다**:

### 데이터가 사라지는 경우:
- ✅ **완전한 재배포 (Rebuild)**: 코드 변경 후 새로운 빌드가 생성되면 파일 시스템이 초기화됨
- ✅ **컨테이너 재시작**: 일부 경우 파일 시스템이 초기화될 수 있음
- ✅ **프로젝트 재배포**: 새 컨테이너로 교체되면 데이터가 사라짐

### 데이터가 유지될 수 있는 경우:
- ⚠️ **단순 재시작**: 같은 컨테이너 내에서 재시작만 하면 데이터가 유지될 수 있음 (보장되지 않음)
- ⚠️ **Redeploy (같은 코드)**: 일부 경우 데이터가 유지될 수 있음 (보장되지 않음)

**결론: 데이터 손실을 방지하려면 영구 저장소가 필요합니다!**

---

## ✅ 해결 방법

### 방법 1: Railway Volumes 사용 (가장 간단)

Railway의 볼륨 기능을 사용하여 `data/` 폴더를 영구 저장소에 연결합니다.

**설정 방법:**
1. Railway 대시보드에서 프로젝트 선택
2. **"+ New"** 또는 **"Add Service"** 클릭
3. **"Volume"** 선택
4. 볼륨 이름 입력 (예: `bot-data`)
5. 마운트 경로 설정: `/app/data`
6. 서비스와 연결

**코드 수정 필요:**
- 볼륨이 마운트되면 자동으로 `data/` 폴더가 영구 저장됨
- 추가 코드 수정 없이 바로 작동!

---

### 방법 2: Railway PostgreSQL 사용 (권장)

Railway의 PostgreSQL 데이터베이스를 사용하여 데이터를 영구 저장합니다.

**설정 방법:**
1. Railway 대시보드에서 프로젝트 선택
2. **"+ New"** 또는 **"Add Service"** 클릭
3. **"Database"** → **"Add PostgreSQL"** 선택
4. PostgreSQL 서비스가 자동으로 생성됨
5. **Variables** 탭에서 `DATABASE_URL` 확인

**코드 수정:**
- `database.js`를 PostgreSQL 사용하도록 변경
- `pg` 패키지 설치 필요

---

### 방법 3: 외부 데이터베이스 사용

- **Supabase** (PostgreSQL, 무료)
- **MongoDB Atlas** (MongoDB, 무료)
- **PlanetScale** (MySQL, 무료)

---

## 🎯 빠른 해결책: Railway Volumes

가장 간단하게 데이터를 유지하려면 **Railway Volumes**를 사용하세요.

### 단계별 가이드:

1. **Railway 대시보드 열기**
   - 프로젝트 선택

2. **Volume 추가**
   - **"+ New"** → **"Volume"** 선택
   - Volume 이름: `bot-data` (원하는 이름)
   - **"Add"** 클릭

3. **서비스에 연결**
   - 봇 서비스(Deployment) 클릭
   - **"Variables"** 탭 클릭
   - 또는 서비스 설정에서 Volume 마운트 경로 설정: `/app/data`

4. **재배포**
   - 코드 변경 없이 바로 사용 가능
   - `data/` 폴더가 볼륨에 자동으로 저장됨

---

## 📊 비교표

| 방법 | 난이도 | 비용 | 데이터 보장 | 설정 시간 |
|------|--------|------|-------------|-----------|
| **현재 (JSON 파일)** | ⭐ | 무료 | ❌ 없음 | 0분 |
| **Railway Volumes** | ⭐⭐ | 무료* | ✅ 높음 | 5분 |
| **Railway PostgreSQL** | ⭐⭐⭐ | 무료* | ✅ 높음 | 15분 |
| **외부 DB (Supabase)** | ⭐⭐⭐ | 무료 | ✅ 높음 | 20분 |

*Railway 무료 크레딧 내에서 사용 가능

---

## 💡 추천

**지금 바로 해야 할 일:**
1. Railway Volumes 설정 (5분 소요)
2. 데이터 손실 걱정 없이 사용 가능!

**나중에 고려할 일:**
- 데이터가 많아지면 PostgreSQL로 마이그레이션
- 더 복잡한 쿼리가 필요하면 데이터베이스 사용

---

## ⚠️ 주의사항

- Railway Volumes는 **프로젝트 삭제 시 데이터가 사라질 수 있음**
- 중요한 데이터는 **정기 백업** 권장
- PostgreSQL이 더 안정적이지만 설정이 복잡함

---

## 🔄 마이그레이션 (데이터가 이미 있는 경우)

1. **현재 데이터 백업**
   - Railway **Logs** 탭에서 `data/` 폴더 내용 확인
   - 또는 로컬에서 데이터 확인

2. **Volume 설정 후**
   - 데이터가 자동으로 볼륨에 저장됨
   - 재배포해도 데이터 유지!

